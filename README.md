# AuthLog
SSH AUthentication Log Analyser
•	Built a Bash script to analyze SSH authentication logs and detect failed login attempts
•	Identified suspicious IPs by counting repeated SSH failures using grep, awk, and sort
•	Implemented threshold-based alerts to flag potential brute-force attacks
•	Added support for custom log files and user-defined date ranges

